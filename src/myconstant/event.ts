export const JOIN_ROOM = "join room";
export const LEAVE_ROOM = "leave room";

export const NAMESPACE = "notification";

export const NOTIFICATIONS = `${NAMESPACE}s`;

export const EVENT_NOTIFICATION = `event-${NAMESPACE}`;
